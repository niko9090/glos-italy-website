# ============================================
# GLOS ITALY FRONTEND - Variabili Ambiente
# ============================================
# Copia questo file come .env.local e compila i valori
#
# DEPLOYMENT VERCEL (glos-italy-website):
# Tutte le variabili marcate [REQUIRED] sono obbligatorie per il deploy
# ============================================

# ============================================
# SANITY CONFIGURATION [REQUIRED]
# ============================================
# Project ID Sanity (lo trovi su sanity.io/manage)
NEXT_PUBLIC_SANITY_PROJECT_ID=97oreljh

# Dataset (di solito "production")
NEXT_PUBLIC_SANITY_DATASET=production

# API Version (formato: YYYY-MM-DD)
NEXT_PUBLIC_SANITY_API_VERSION=2024-01-01

# URL dello Studio Sanity per visual editing
NEXT_PUBLIC_SANITY_STUDIO_URL=https://glositalystudio.vercel.app

# ============================================
# SANITY TOKENS [REQUIRED for Preview/Revalidation]
# ============================================
# API Token per preview mode e query con draft
# Genera su: sanity.io/manage -> API -> Tokens
# Permessi richiesti: Viewer o Editor
SANITY_API_TOKEN=your-sanity-api-token

# Secret per webhook di revalidazione
# Genera una stringa random sicura (es: openssl rand -base64 32)
# Deve corrispondere al secret configurato in Sanity webhook
SANITY_REVALIDATE_SECRET=your-revalidation-secret

# Secret per preview mode (opzionale, per sicurezza extra)
# Se configurato, le richieste di preview devono includere questo secret
SANITY_PREVIEW_SECRET=your-preview-secret

# ============================================
# SITE CONFIGURATION
# ============================================
# URL pubblico del sito (usato per metadata, sitemap, ecc.)
NEXT_PUBLIC_SITE_URL=https://glositaly.vercel.app

# ============================================
# EMAIL CONFIGURATION (opzionale)
# ============================================
# API Key Resend per invio email dal form contatti
# Registrati su: resend.com
RESEND_API_KEY=your-resend-api-key

# Email destinatario per form contatti
CONTACT_EMAIL=info@glositaly.it
